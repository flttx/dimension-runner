# 性能优化清单

## 指标与诊断

- 记录 Web Vitals（LCP/INP/CLS）。
- 用 Lighthouse 与性能时间线定位瓶颈。
- 识别最慢路由与最重的组件树。

## 资源与包体

- 使用 `next/dynamic` 拆分非关键模块。
- 图片统一 `next/image` 与合适尺寸。
- 字体使用 `next/font` 并减少变体。

## 渲染与交互

- 减少无效重渲染，拆分高频状态。
- 大列表启用虚拟化。
- 降低动画/阴影/滤镜的范围与频率。

## 缓存与数据

- API 请求引入缓存与去重。
- 优先 SSR/RSC 以减少客户端负担。
