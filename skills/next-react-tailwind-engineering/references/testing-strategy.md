# 测试策略

## 分层

- 单元测试：逻辑函数、工具、hooks。
- 组件测试：关键 UI 与交互。
- 端到端：核心流程与回归。

## 组织方式

- `src/__tests__/` 或与源码同级的 `*.test.ts(x)`。
- 业务逻辑优先测，展示组件按需覆盖。

## 运行原则

- 本地至少跑单测；提交前确保 `lint` 与 `test` 通过。
- CI 保持最小必需步骤，避免过度构建。
